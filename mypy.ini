[mypy]

# apscheduler ships no type stubs and no types-apscheduler package exists
# on PyPI for this version; everything else in the project has real stubs
# (types-requests) or is first-party, so this stays a narrow, named
# exception rather than a blanket ignore_missing_imports.
[mypy-apscheduler.*]
ignore_missing_imports = True
