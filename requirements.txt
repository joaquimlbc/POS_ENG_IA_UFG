# FastAPI e Web Server
# fastapi/starlette/anyio/httpx pinned together as a known-compatible set -
# leaving any of these unpinned lets pip resolve a newer starlette/anyio pair
# where starlette.testclient emits a DeprecationWarning (anyio.abc.BlockingPortal
# alias, or later, httpx vs "httpx2") that pytest.ini's filterwarnings=error
# turns into a hard collection failure (exit code 4) in CI.
fastapi==0.115.6
starlette==0.41.3
anyio==4.6.2.post1
httpx==0.27.2
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Banco de Dados
sqlalchemy==2.0.23
alembic==1.12.1

# Agendamento de tarefas (sync diário - US-012)
apscheduler==3.11.3

# Frontend
streamlit==1.51.0
pandas==2.3.3
plotly==7.1.0

# Requisições HTTP
requests==2.31.0

# Variáveis de Ambiente
python-dotenv==1.0.0

# Utilitários
python-dateutil==2.8.2

# Desenvolvimento e Testes
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.0
flake8==6.1.0
mypy==1.7.1
